import { Loader } from '@mantine/core';

const LoadingModal = () => {
  return (
    <div className="group jc-center ai-center">
      <Loader color="black" />
    </div>
  );
};

export default LoadingModal;
